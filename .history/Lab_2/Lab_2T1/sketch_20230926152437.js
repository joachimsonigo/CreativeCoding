function setup() {
  c = createCanvas(displayWidth, displayHeight);
  c.position(0, 0);
}

function draw() {
  background(220);
}

// function resize() {
//   resizeCanvas(windowWidth, windowHeight);
// }